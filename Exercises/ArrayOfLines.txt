// Max and min lengths of lines in the array

double max = Line.length(lineArr[0]);
int maxPlace = -1;
double min = Line.length(lineArr[0]);

for(int i = 0; i < lineArr.length; i++){
    if(Line.length(lineArr[i] > max)){
        max = Line.length(lineArr[i]);
        maxPlace = i;
    }
    
    if(Line.length(lineArr[i] < min))
        min = Line.length(lineArr[i]);
}

